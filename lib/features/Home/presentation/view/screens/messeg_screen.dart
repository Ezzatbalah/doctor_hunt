import 'package:flutter/material.dart';

class MessegScreen extends StatelessWidget {
  const MessegScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MessegScreen", style: TextStyle(fontSize: 20)),
    );
  }
}
